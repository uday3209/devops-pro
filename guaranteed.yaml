apiVersion: v1
kind: Pod
metadata:
  name: guaranteed

spec:
  containers:
  - name: guaranteed
    image: nginx
    resources:
      limits:
        cpu: "700m"
        memory: "200Mi"
      requests:
        cpu: "700m"
        memory: "200Mi"
